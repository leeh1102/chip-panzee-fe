import React from 'react';
import PropTypes from 'prop-types';
import styles from './PostEditComponent.module.css';

const PostEditComponent = () => (
  <div className={styles.PostEditComponent}>
    PostEditComponent Component
  </div>
);

PostEditComponent.propTypes = {};

PostEditComponent.defaultProps = {};

export default PostEditComponent;
